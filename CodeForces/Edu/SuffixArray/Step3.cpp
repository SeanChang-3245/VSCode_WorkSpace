#include <bits/stdc++.h>
using namespace std;

// substring searching using suffix array


int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);

    string str;
    int len;
    cin >> str;
    len = str.size();

    int q;
    cin >> q;

    while(q--)
    {
        string k;
        cin >> k;

        vector<string> vec;


    }
}