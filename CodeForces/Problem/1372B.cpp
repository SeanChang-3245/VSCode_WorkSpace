#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define endl '\n'

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);

    int t;
    cin >> t;
    list<int> prime;
    vector<int> lps(10e9);

    while(t--)
    {
        ll tmp;
        cin >> tmp;

        
    }
    return 0;
}